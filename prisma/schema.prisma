// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Users {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name  String    @default("null")
  createdAt DateTime @default(now())
}

model Users2 {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  surname   String
  createdAt DateTime @default(now())
}

model ExampleTable {
  id          Int      @id @default(autoincrement())
  age         Int
  price       Float
  description String
  createdAt   DateTime @default(now())
}

model testTableTurbo {
  id    Int      @id @default(autoincrement())
  date  DateTime
  email String
}

model ExampleTable4 {
  id          Int      @id @default(autoincrement())
  age         Int
  price       Float
  description String
  createdAt   DateTime @default(now())
}

model ExampleTable5 {
  id          Int      @id @default(autoincrement())
  age         Int
  price       Float
  description String
  createdAt   DateTime @default(now())
}

model ExampleTable6 {
  id          Int      @id @default(autoincrement())
  age         Int
  price       Float
  description String
  createdAt   DateTime @default(now())
}

model test3 {
  id    Int   @id @default(autoincrement())
  hello Float
}

model testTableTurbow {
  id     Int   @id @default(autoincrement())
  testtt Float
}

model hello1 {
  id   Int    @id @default(autoincrement())
  test String
}

model BOMBOCLATTTT {
  id   Int    @id @default(autoincrement())
  amen String
}

model lastOneWorkPls {
  id   Int    @id @default(autoincrement())
  test String
}
